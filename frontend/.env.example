# Network: "testnet" (default) or "public"
VITE_NETWORK=testnet

# Deployed contract addresses (filled in after running scripts/deploy-testnet.sh)
VITE_SAVINGS_CIRCLE_CONTRACT_ID=
VITE_PENALTY_HANDLER_CONTRACT_ID=

# Testnet SAC token used for deposits and collateral
# (issue your own, or use an existing one on testnet)
VITE_CIRCLE_TOKEN_CONTRACT_ID=
